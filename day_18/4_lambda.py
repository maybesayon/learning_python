# Lambda in python are the element to create a on-line functions
# These functions do not have names. They are also called anonymous function

sum = lambda a, b: a + b
print(sum(4, 5))

is_even = lambda x: x % 2 ==0
print(is_even(4))
